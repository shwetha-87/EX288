{{- if .Values.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "customer-backend.fullname" . }}
spec:
  to:
    kind: Service
    name: {{ include "customer-backend.fullname" . }}
  port:
    targetPort: 8080
  {{- if eq .Values.secureTransport "true" }}
  tls:
    termination: edge
  {{- end }}
{{- end }}
